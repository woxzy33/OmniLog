const fs = require("fs"); let c = fs.readFileSync("src/components/ProfileTab.jsx", "utf8"); const lines = c.split("\n"); lines[559] = "                <span style={{ fontSize: 13, color: \"#8b90a0\", marginTop: 4, fontWeight: 600 }}>{e.category ? t(`categories.${e.category.toLowerCase()}`, e.category) : \"\"} {e.usageCount > 0 && `• Done ${e.usageCount}x`}</span>"; fs.writeFileSync("src/components/ProfileTab.jsx", lines.join("\n"));
