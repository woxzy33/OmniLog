const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('LOG:', msg.text())); page.on('pageerror', err => console.log('ERR:', err.message)); await page.goto('http://localhost:5173'); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const btns = Array.from(document.querySelectorAll('button')); const p = btns.find(b => b.innerText.includes('Profile')); if(p) { console.log('Clicked Profile'); p.click(); } }); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const btns = Array.from(document.querySelectorAll('button')); const e = btns.find(b => b.innerText.includes('Exercises')); if(e) { console.log('Clicked Exercises'); e.click(); } }); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const btns = Array.from(document.querySelectorAll('button')); const b = btns.find(b => b.innerText.includes('Back') || b.innerText.includes('Cancel') || b.innerText.includes('Back') || b.innerText.includes('Back')); if (b) b.click(); else history.back(); }); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const btns = Array.from(document.querySelectorAll('button')); const m = btns.find(b => b.innerText.includes('Measures')); if(m) { console.log('Clicked Measures'); m.click(); } }); await new Promise(r => setTimeout(r, 1000)); await browser.close(); })();
