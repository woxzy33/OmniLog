const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('LOG:', msg.text())); page.on('pageerror', err => console.log('ERR:', err.message)); await page.goto('http://localhost:5173'); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const tabs = document.querySelectorAll('button'); for(let b of tabs) { if(b.innerText.includes('History') || b.innerText.includes('Profile')) b.click(); } }); await new Promise(r => setTimeout(r, 2000)); await browser.close(); })();
