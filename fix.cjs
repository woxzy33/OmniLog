const fs = require("fs"); let c = fs.readFileSync("src/components/ProfileTab.jsx", "utf8"); c = c.replace("      </div>\n  const [selectedMetric, setSelectedMetric] = useState(\"weight\");", "      </div>\n    </motion.div>\n  );\n}\n\nfunction MeasuresView({ data, persist, onBack, settings }) {\n  const [selectedMetric, setSelectedMetric] = useState(\"weight\");"); fs.writeFileSync("src/components/ProfileTab.jsx", c);
