const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173'); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const btns = Array.from(document.querySelectorAll('button')); const p = btns.find(b => b.innerText.includes('Profile')); if(p) p.click(); }); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const btns = Array.from(document.querySelectorAll('button')); const e = btns.find(b => b.innerText.includes('Exercises')); if(e) e.click(); }); await new Promise(r => setTimeout(r, 1000)); await page.screenshot({path: 'exercises.png'}); await page.evaluate(() => { const btns = Array.from(document.querySelectorAll('button')); const b = btns.find(b => b.innerText.includes('Back') || b.querySelector('svg')); if (b) b.click(); }); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const btns = Array.from(document.querySelectorAll('button')); const m = btns.find(b => b.innerText.includes('Measures')); if(m) m.click(); }); await new Promise(r => setTimeout(r, 1000)); await page.screenshot({path: 'measures.png'}); await browser.close(); })();
